services:
  auth:
    build:
      context: ./auth
      dockerfile: Dockerfile
    environment:
      - CONFIG_PATH=/app/config/production.yaml
    ports:
      - "44044:44044"
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"